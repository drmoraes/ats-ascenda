/**
 * @deprecated Movido para `common/tenancy/tenant-resolver.service.ts` e exposto
 * pelo `TenancyModule` (@Global). Este arquivo é apenas um encaminhamento para
 * não quebrar imports legados e pode ser removido com segurança.
 */
export { TenantResolverService } from '../../common/tenancy/tenant-resolver.service';
